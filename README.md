# Noted the work shall pass as it is working on.

### src/config.py
- This includes all the hardcored setting and file path.

### src/text_egine.py
**librareis used:**
 `re` for the regex for the striping.
 `datetime` and `nepali_datetime` : for converting the nepali date and english date as the citizenship contain both the AD and BS format.
 `SequenceMatcher` A python library used for the comparision of two string and gives a precentage of how similar they are.

`unicodedata`: use for te